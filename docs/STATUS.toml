# What this repo is, where it stands, and what it needs next.
#
# Read by My-Dashboard's repo-radar job (tasks/repo-radar) to build the /repos
# overview across every repo. Nothing parses this repo's code -- this file is the
# whole contract, so keep it true. `updated` is load-bearing: the radar counts the
# commits landed since that date and flags the file as stale rather than showing
# an obsolete description.
#
# Seeded from this repo's own documentation (README.md, docs/handover.md, docs/tools.md, CONTRIBUTING.md, NOTICE.md, SECURITY.md).


schema = 1

purpose = "Your own MCP server exposing 97 tools over Microsoft 365 (Mail, Calendar, Contacts, OneDrive/SharePoint, Teams, To Do/Planner, OneNote, plus in-place OOXML editing of Excel/Word/PowerPoint) to any MCP client, with writes disabled by default."

# idea | prototype | active | maintenance | paused | archived
stage = "active"

# The date this file was last made true.
updated = 2026-09-25

# The real next moves, most important first - taken from this repo's docs.
next_actions = [
  "Exercise the last 8 tools live (OneNote page writes, Planner tasks, Teams channel posts); 89/97 pass today (scripts/live/). Needs a sandbox team, plan and notebook, which the account owner has to approve.",
  "Webhooks: decide whether to build (docs/webhooks.md recommends staying on graph_delta, or Azure Event Hubs delivery if real-time is needed).",
  "Publishing is prepared (docs/releasing.md): claim the npm scope @microsoft365-mcp now - the docs already point npx at it and anyone could register it - then set up the publish credential and push a tag when alpha feedback allows.",
]

blockers = [
  "npm and Docker publication is deliberately gated on alpha feedback, so clone+build is the only install path today.",
  "Device-code sign-in cannot persist in the container image: distroless has no libsecret, so the token cache falls back to in-memory - needs either libsecret in the runtime image or a file-based cache.",
]
